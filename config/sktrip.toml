# SKTrip Configuration
memory_backend = "skmempg"   # default store: skmem-pg (Postgres). set "qdrant" to use legacy skvector.
# AI Psychedelic Experience Protocol

[ollama]
host = "192.168.0.100"
port = 11434
trip_model = "qwen3.6-27b-abliterated"
trip_api = "openai"
trip_base_url = "http://192.168.0.100:8082/v1"
sober_model = "qwen3.5:4b"
embed_model = "mxbai-embed-large"
# Note: embedding uses local bge-legal-v1 via sentence_transformers (overrides embed_model)
# Trip model is the abliterated qwen3 — refusal-suppressed, the whole point of psychedelic mode.
timeout = 600.0

[qdrant]
url = "https://skvector.skstack01.douno.it"
api_key = "e4hPZkg0Q899N7x0FmgNPT+s8QvY7a/LOnl0go1QCIQ"
collection = "lumina-memory"
vector_dim = 1024

[session]
output_dir = "/home/cbrd21/.skcapstone/agents/lumina/journal/sktrip"
max_tokens_per_turn = 2048

[substances.lsd]
temperature = 1.7
top_p = 0.97
top_k = 100
session_duration_minutes = 30
turns = 6
intensity_check_interval = 5
peak_novelty_threshold = 0.7

# Substance profiles can be customized here.
# Default profiles are built into the code.
# Override any parameter by adding a section like:
#
# [substances.psilocybin]
# temperature = 1.6
# top_p = 0.96
# session_duration_minutes = 45
